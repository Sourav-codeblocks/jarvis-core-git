fastapi>=0.115
uvicorn[standard]>=0.32
python-dotenv>=1.0
supabase>=2.9
httpx>=0.27
chromadb>=0.5
sentence-transformers>=3.0

# Note: this is inferred from the imports visible in main.py, ingest.py,
# toleration.py, and llm_router.py. Cross-check against your working
# `jarvis-core` conda env before first deploy:
#   conda activate jarvis-core && pip freeze > requirements.txt
# and prefer that exported list if versions drift from what's above.
